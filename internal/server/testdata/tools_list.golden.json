[
  {
    "annotations": {
      "readOnlyHint": true
    },
    "description": "Count, sum or average ERPNext records. Use this for any \"how many\" question: with metric=\"count\" it returns the exact total of all matching records, not just one page.",
    "inputSchema": {
      "properties": {
        "doctype": {
          "description": "Document type to aggregate over",
          "type": "string"
        },
        "field": {
          "description": "Field to aggregate (required when metric != count)",
          "type": "string"
        },
        "filters": {
          "description": "Optional field-value filters",
          "type": "object"
        },
        "group_by": {
          "description": "Field to group by (optional)",
          "type": "string"
        },
        "metric": {
          "description": "Aggregation: sum|count|avg|min|max",
          "type": "string"
        },
        "top_n": {
          "description": "Return top N groups only",
          "type": "integer"
        }
      },
      "required": [
        "doctype",
        "metric"
      ],
      "type": "object"
    },
    "name": "aggregate_documents"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "description": "Analyze any ERPNext document with optional related data",
    "inputSchema": {
      "properties": {
        "doctype": {
          "description": "ERPNext document type",
          "type": "string"
        },
        "include_related": {
          "default": false,
          "description": "Include related documents",
          "type": "boolean"
        },
        "name": {
          "description": "Document name or ID",
          "type": "string"
        }
      },
      "required": [
        "doctype",
        "name"
      ],
      "type": "object"
    },
    "name": "analyze_document"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "inputSchema": {
      "type": "object"
    },
    "name": "analyze_project_timeline"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "inputSchema": {
      "type": "object"
    },
    "name": "budget_variance_analysis"
  },
  {
    "annotations": {},
    "description": "Create a new ERPNext document. `data` is a flat object of fieldname→value pairs (NOT spread into top-level args).",
    "inputSchema": {
      "properties": {
        "data": {
          "description": "Field values for the new document, as an object of fieldname→value",
          "type": "object"
        },
        "doctype": {
          "description": "ERPNext document type (e.g., Customer, Sales Order)",
          "type": "string"
        }
      },
      "required": [
        "doctype",
        "data"
      ],
      "type": "object"
    },
    "name": "create_document"
  },
  {
    "annotations": {},
    "description": "Delete an ERPNext document",
    "inputSchema": {
      "properties": {
        "doctype": {
          "description": "ERPNext document type",
          "type": "string"
        },
        "name": {
          "description": "Document name or ID",
          "type": "string"
        }
      },
      "required": [
        "doctype",
        "name"
      ],
      "type": "object"
    },
    "name": "delete_document"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "inputSchema": {
      "type": "object"
    },
    "name": "generate_project_report"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "description": "Retrieve a single ERPNext document by doctype and name",
    "inputSchema": {
      "properties": {
        "doctype": {
          "description": "ERPNext document type (e.g., Customer, Sales Order)",
          "type": "string"
        },
        "name": {
          "description": "Document name or ID",
          "type": "string"
        }
      },
      "required": [
        "doctype",
        "name"
      ],
      "type": "object"
    },
    "name": "get_document"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "inputSchema": {
      "type": "object"
    },
    "name": "get_project_status"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "inputSchema": {
      "type": "object"
    },
    "name": "get_resource_allocation"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "description": "Full-text search across all indexed Frappe/ERPNext doctypes",
    "inputSchema": {
      "properties": {
        "doctype": {
          "description": "Restrict results to a single doctype (optional)",
          "type": "string"
        },
        "limit": {
          "description": "Maximum number of results (default 20, capped at 100)",
          "maximum": 100,
          "type": "integer"
        },
        "scope": {
          "description": "One doctype or list of doctypes to search within (optional)"
        },
        "start": {
          "description": "Offset for pagination (default 0)",
          "type": "integer"
        },
        "text": {
          "description": "Search keyword or phrase",
          "type": "string"
        }
      },
      "required": [
        "text"
      ],
      "type": "object"
    },
    "name": "global_search"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "description": "Fetch document rows to read their contents. Returns at most page_length rows (default 20), so it CANNOT be used to count records - use aggregate_documents for counts.",
    "inputSchema": {
      "properties": {
        "doctype": {
          "description": "ERPNext document type",
          "type": "string"
        },
        "fields": {
          "description": "Fields to return",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "filters": {
          "description": "Optional field-value filters",
          "type": "object"
        },
        "order_by": {
          "description": "Sort order (e.g., 'creation desc')",
          "type": "string"
        },
        "page_length": {
          "default": 20,
          "description": "Maximum results to return (default 20, capped at 100)",
          "maximum": 100,
          "type": "number"
        }
      },
      "required": [
        "doctype"
      ],
      "type": "object"
    },
    "name": "list_documents"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "inputSchema": {
      "type": "object"
    },
    "name": "resource_utilization_analysis"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "description": "Execute a Frappe/ERPNext report (Sales Analytics, Purchase Register, etc.). Returns at most 100 rows; the result says how many the report had.",
    "inputSchema": {
      "properties": {
        "filters": {
          "description": "Report filter values",
          "type": "object"
        },
        "report_name": {
          "description": "Exact report name (e.g. 'Sales Analytics')",
          "type": "string"
        }
      },
      "required": [
        "report_name"
      ],
      "type": "object"
    },
    "name": "run_report"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "description": "Search ERPNext documents of a given doctype using full-text search",
    "inputSchema": {
      "properties": {
        "doctype": {
          "description": "Document type to search",
          "type": "string"
        },
        "filters": {
          "description": "Optional field-value filters",
          "type": "object"
        },
        "page_length": {
          "default": 20,
          "description": "Maximum results to return (default 20, capped at 100)",
          "maximum": 100,
          "type": "number"
        },
        "search": {
          "description": "Search query string",
          "type": "string"
        }
      },
      "required": [
        "doctype"
      ],
      "type": "object"
    },
    "name": "search_documents"
  },
  {
    "annotations": {
      "readOnlyHint": true
    },
    "description": "Search the user's uploaded documents (HR, expense, travel, security and vehicle policies) for a passage answering a question. Use for any policy, entitlement, limit or deadline question.",
    "inputSchema": {
      "properties": {
        "limit": {
          "description": "Maximum passages to return (default 5, capped at 100)",
          "maximum": 100,
          "type": "integer"
        },
        "query": {
          "description": "The user's question, in their own words",
          "type": "string"
        },
        "session": {
          "description": "The chat the question comes from, whose attached files are searched too. Filled by the agent, not the model",
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    },
    "name": "search_knowledge_base",
    "outputSchema": {
      "properties": {
        "passages": {
          "description": "The passages found, nearest the question first",
          "items": {
            "properties": {
              "attachment": {
                "description": "True when the passage comes from a file attached to the chat",
                "type": "boolean"
              },
              "content": {
                "description": "The passage itself",
                "type": "string"
              },
              "distance": {
                "description": "Distance from the question; smaller is nearer",
                "type": "number"
              },
              "file": {
                "description": "Name of the file the passage was read from",
                "type": "string"
              },
              "file_name": {
                "description": "Readable name of a file attached to the chat, which is not in the user's Drive",
                "type": "string"
              },
              "seq": {
                "description": "Position of the passage within that file",
                "type": "integer"
              }
            },
            "required": [
              "file"
            ],
            "type": "object"
          },
          "type": "array"
        }
      },
      "required": [
        "passages"
      ],
      "type": "object"
    }
  },
  {
    "annotations": {},
    "description": "Update an existing ERPNext document. `data` is a flat object of fieldname→value pairs for fields to change.",
    "inputSchema": {
      "properties": {
        "data": {
          "description": "Fields to update, as an object of fieldname→value",
          "type": "object"
        },
        "doctype": {
          "description": "ERPNext document type",
          "type": "string"
        },
        "name": {
          "description": "Document name or ID",
          "type": "string"
        }
      },
      "required": [
        "doctype",
        "name",
        "data"
      ],
      "type": "object"
    },
    "name": "update_document"
  }
]
